# Local development (Vite proxies /api/* to http://localhost:3000):
VITE_API_URL=/api
#
# Production always uses /api regardless of VITE_API_URL. frontend/vercel.json
# proxies it to the backend so the refresh-token cookie remains first-party.
# VITE_API_URL is only honored by development builds for local overrides.
